# Bangkok Bus Tracker

Multilingual (EN/MM/TH) Bangkok city bus guide with GTFS-based routes.

## Features
- Static routes from GTFS → JSON
- Multilingual UI (English, Myanmar, Thai)
- CLI app
- Kivy mobile UI (Android-ready)
- Google Maps route open

## Setup

```bash
pip install -r requirements.txt
